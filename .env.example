# Copy this file to .env.local and fill in real values.
# .env.local is git-ignored — never commit real secrets.
#
# ─────────────────────────────────────────────────────────────────────────────
# NOTE — two different files:
#   .env.local  → secrets below, loaded INTO the backend container (env_file)
#   .env        → deployment settings, read BY Docker Compose for ${...} in
#                 docker-compose.yml. Both are git-ignored.
#
# Local dev needs no .env at all — the defaults cover it. On a deployed host
# (e.g. a GCP VM behind a domain) create a root .env like:
#
#   FRONTEND_PORT=80
#   PUBLIC_URL=http://assignment-hub.mooo.com
#
# FRONTEND_PORT is the host port mapped to Vite's 5173 (80 = bare domain, no
# ":5173" in the URL). PUBLIC_URL is the origin the browser uses; it builds the
# OAuth redirect URIs, so it must match what's registered with Google/Azure
# exactly. Also add the hostname to `server.allowedHosts` in
# frontend/vite.config.js, or Vite rejects the request.
# ─────────────────────────────────────────────────────────────────────────────

# --- Google OAuth ---
# Google Cloud Console → APIs & Services → Credentials → OAuth client ID (Web application)
# Authorized redirect URI: http://localhost:5173/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# --- Microsoft OAuth ---
# Azure Portal → Azure Active Directory → App registrations → New registration
# Redirect URI (Web): http://localhost:5173/api/auth/microsoft/callback
# MS_CLIENT_ID    = "Application (client) ID" on the app's Overview page
# MS_CLIENT_SECRET = the secret's "Value" from Certificates & secrets (not the Secret ID)
# MS_TENANT_ID    = "Directory (tenant) ID" on the app's Overview page
#   (only needed if the app is single-tenant; omit to default to "organizations")
MS_CLIENT_ID=
MS_CLIENT_SECRET=
MS_TENANT_ID=

# --- Session ---
# Any long random string, e.g. generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=
